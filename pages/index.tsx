interface Props {
  title: string;
};

const Home: React.FC<Props> = ({ title = 'NoxTube' }): JSX.Element => {
  return (
    <section className="flex w-full mt-[55px]">

    </section>
  );
};

export default Home;
